{
  "name": "whatsapp-coach",
  "private": true,
  "type": "module",
  "description": "n8n workflow: a daily coaching check-in on WhatsApp - set the goal, record the day, coach",
  "scripts": {
    "test": "node --test \"test/*.test.mjs\"",
    "sync": "node sync-code.mjs"
  },
  "devDependencies": {
    "luxon": "^3.5.0"
  }
}
