{
  "name": "smart-scale-os-whatsapp",
  "private": true,
  "type": "module",
  "description": "n8n workflow: the Smart Scale OS daily loop on WhatsApp - set the goal, record the day, coach",
  "scripts": {
    "test": "node --test \"test/*.test.mjs\"",
    "sync": "node sync-code.mjs"
  },
  "devDependencies": {
    "luxon": "^3.5.0"
  }
}
