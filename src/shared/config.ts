/** @file Constants every lane must agree on. Change them here and nowhere else. */

export const ZONE = 'Europe/Helsinki';

/** Replies before this local hour belong to the previous day. */
export const DAY_ROLLOVER_HOUR = 4;

export const DATE_FORMAT = 'yyyy-MM-dd';
